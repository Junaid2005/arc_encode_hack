"""Frontend components package for Streamlit app."""

# This file ensures Python treats `components` as a regular package so imports
# like `components.chatbot_lib` resolve correctly when Streamlit runs from the
# app directory.



